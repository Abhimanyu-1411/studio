
'use client';

// This component is no longer used for navigation, as it has been consolidated into the Header.
// It can be removed or kept for potential future use as an icon-only sidebar on desktop.
// For now, returning null to prevent it from rendering.
export function Sidebar() {
  return null;
}
